import java.util.Scanner;
public class Project1 {

    
   // It should consist of the following four class (static) methods:
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        //how to select the input file from the default directory by using the JFileChooser class and store in array
        //make an array type Weight
        //call remaining 3 methods to find max, min and ave
        //***file should have >25 weights(1 per line!) of display error

    }

   private int findMinimum(){
       return 0;
       
    //    findMinimum:
    //    1.accepts the array of weights as a parameter together
    //    with the number of valid weights it contains

    //    2.return the smallest weight in that array.
   }
    
   private int findMaximum(){
        return 0;
    
    // findMaximum:
    // 1.that accepts the array of weights as a parameter
    // together with the number of valid weights it contains.
    // 2.return the largest weight in that array
    
   }

    private int findAverage(){
         return 0;
        // findAverage:
        // 1.that accepts the array of weights as a parameter together with
        // the number of valid weights it contains.
        // 2.return the average of all the weights in that array.
    }
}
